# Bluesky Ranker configuration template
# Copy this file to blueskyranker/.env and fill in your secrets.

# Feed generator credentials
FEEDGEN_HOSTNAME=feed.example.org
PRIORITIZE_API_KEY=replace-with-api-key
# Optional: override when the feed generator listens elsewhere (e.g., docker compose)
# FEEDGEN_LISTENHOST=localhost:3020

# Optional SBERT cache for offline usage
# SBERT_MODEL_PATH=/path/to/.cache/huggingface/hub
# SBERT_LOCAL_ONLY=1
